numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
scipy>=1.10
joblib>=1.3
matplotlib>=3.7

# Optional — only needed for the symbolic-regression arm (RUN_PYSR=1).
# Requires a working Julia install; see https://github.com/MilesCranmer/PySR
# pysr>=0.18
